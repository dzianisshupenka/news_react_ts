export interface INews {
    title: string
    text: string
    author: string
    keywords: Array<string>
    date: number
};

export interface IUsers {
    nickname: string
    firstName: string
    lastNane: string
    email: string
    password: string
}
